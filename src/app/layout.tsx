import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "Dreamhouse | AI Home Discovery for Agents",
  description:
    "AI-powered home discovery engine for buyer agents. Search on-market and off-market properties using natural language. Find your buyer's dream home faster.",
  openGraph: {
    title: "Dreamhouse | AI Home Discovery for Agents",
    description:
      "AI-powered home discovery engine for buyer agents. Search on-market and off-market properties using natural language.",
    type: "website",
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="en">
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
